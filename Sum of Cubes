#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        long long x;
        cin >> x;
        long long a = 1, b = pow(x, 1.0/3) + 1;
        bool ok = false;
        while (a <= b) {
            long long sum = a*a*a + b*b*b;
            if (sum == x) { ok = true; break; }
            else if (sum < x) a++;
            else b--;
        }
        cout << (ok ? "YES" : "NO") << "\n";
    }
}
