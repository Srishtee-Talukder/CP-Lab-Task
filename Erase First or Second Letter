#include <bits/stdc++.h>
using namespace std;

int main() {
    int t; cin >> t;
    while(t--) {
        int n; string s;
        cin >> n >> s;
        set<string> st;
        for(int i=0;i<n;i++)
            st.insert(s.substr(i));
        cout << st.size() << "\n";
    }
}
